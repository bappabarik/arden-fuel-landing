# --- ArdenBackend API (contact form submits here) ---
# Local dev default is http://localhost:4000 if unset. In production, point this at the deployed
# ArdenBackend URL, and make sure that backend's CORS_ORIGIN includes this site's domain.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
